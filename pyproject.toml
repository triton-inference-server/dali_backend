[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [
  ".cmake-format.py",
  "benchmarks/dali_vs_python/BM_jasper/model_repository/jasper_python/1/features.py",
  "extern",
]

[tool.ruff.lint]
# Start with high-signal syntax and undefined-name checks. Broader style rules
# would require an unrelated repository-wide cleanup.
select = ["E9", "F63", "F7", "F82"]
